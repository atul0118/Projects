* This is a client server setup that is implemented using linux IPC, POSIX threads.
* Server can take request from clients to either perform substraction or addition and return
  the result to respective user. The client code be easily modified to run as many clients as we want.
